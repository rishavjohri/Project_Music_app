import 'package:flutter/material.dart';
class construction extends StatelessWidget {
  const construction({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text("Construction Page"),),
      ),
    );
  }
}
